node_modules
dist
.output
.vinxi
.nitro
.wrangler
pnpm-lock.yaml
package-lock.json
bun.lock

# ---------------------------------------------------------------------------
# Generated sources — owned by a generator, not by us.
# ---------------------------------------------------------------------------
#
# Formatting these is not merely pointless, it is actively harmful: each is
# rewritten verbatim by its generator, so a formatted copy survives only until
# the next run and then reappears unformatted — failing `npm run lint` for a
# change nobody made. Leaving them alone keeps the generator authoritative.
#
# Paths are spelled without brackets on purpose: .prettierignore uses gitignore
# glob semantics, where `[.mcp]` is a character class rather than a directory
# name, so the literal path would never match.

# TanStack Router route tree.
routeTree.gen.ts

# Lovable Cloud Supabase integration ("This file is automatically generated").
src/integrations/supabase/

# Lovable's own project metadata, including the MCP manifest that its Vite
# plugin re-emits alongside the route handlers below.
.lovable/

# Entirely a LOVABLE:BEGIN/END managed block, rewritten by Lovable on sync.
AGENTS.md

# @lovable.dev/mcp-js route handlers, re-emitted by its Vite plugin on every
# dev server start and every build.
src/routes/mcp.ts
src/routes/**/list-tools.ts
src/routes/**/invoke-tool/**
src/routes/**/oauth-protected-resource.ts
